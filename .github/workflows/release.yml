name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    name: create GitHub release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: verify tests pass at tag
        run: go test ./...

      - name: extract changelog section
        id: changelog
        run: |
          tag="${GITHUB_REF_NAME}"
          # Pull the notes for this version out of CHANGELOG.md if present,
          # otherwise fall back to a generic message.
          awk -v ver="${tag#v}" '
            $0 ~ "^## \\[" ver "\\]" {grab=1; next}
            grab && /^## \[/ {exit}
            grab {print}
          ' CHANGELOG.md > RELEASE_NOTES.md
          if [ ! -s RELEASE_NOTES.md ]; then
            echo "Release ${tag}." > RELEASE_NOTES.md
          fi

      - name: create release
        uses: softprops/action-gh-release@v2
        with:
          body_path: RELEASE_NOTES.md
          prerelease: ${{ startsWith(github.ref_name, 'v0.') }}

# NOTE: GoReleaser (binary builds for the scs / scs-web CLIs and a Docker image)
# is added in Batch 2, when the studio and its commands land. The v0.x library
# releases above are tag-and-notes only.
