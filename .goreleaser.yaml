# GoReleaser configuration for the fate CLIs (scs, scs-web).
# Tagging vX.Y.Z triggers .github/workflows/release.yml, which runs goreleaser
# to build cross-platform binaries, archives, and checksums, and publishes them
# on the GitHub release.
version: 2

project_name: fate

before:
  hooks:
    - go mod tidy

builds:
  - id: scs
    main: ./cmd/scs
    binary: scs
    env: [CGO_ENABLED=0]
    flags: [-trimpath]
    ldflags: ["-s -w"]
    goos: [linux, darwin]
    goarch: [amd64, arm64]
  - id: scs-web
    main: ./cmd/scs-web
    binary: scs-web
    env: [CGO_ENABLED=0]
    flags: [-trimpath]
    ldflags: ["-s -w"]
    goos: [linux, darwin]
    goarch: [amd64, arm64]

archives:
  - id: default
    ids: [scs, scs-web]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

changelog:
  use: github
  sort: asc

release:
  prerelease: auto
